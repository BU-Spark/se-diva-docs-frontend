import * as ra_core from 'ra-core';
import { DataProvider } from 'ra-core';

declare const _default: (apiUrl: any, httpClient?: (url: any, options?: ra_core.Options) => Promise<{
    status: number;
    headers: Headers;
    body: string;
    json: any;
}>) => DataProvider;

export { _default as default };
