import * as React from 'react';
import { Store } from './types';
export declare const StoreContextProvider: ({ value: Store, children, }: StoreContextProviderProps) => JSX.Element;
export interface StoreContextProviderProps {
    value: Store;
    children: React.ReactNode;
}
//# sourceMappingURL=StoreContextProvider.d.ts.map