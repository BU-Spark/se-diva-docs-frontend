import * as ra_core from 'ra-core';
import { DataProvider } from 'ra-core';

declare const _default: (apiUrl: string, httpClient?: (url: any, options?: ra_core.Options) => Promise<{
    status: number;
    headers: Headers;
    body: string;
    json: any;
}>, countHeader?: string) => DataProvider;

export { _default as default };
